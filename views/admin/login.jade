extends layouts/main

block content
    h1= Login
    form(name="add-estimation", method="post", id="login-form" action="login")
        div.input
            span.label name
            input(type="text", name="username")
        div.input
            span.label password
            input(type="text", name="password")
        div.actions
            input(type="submit", value="Login")